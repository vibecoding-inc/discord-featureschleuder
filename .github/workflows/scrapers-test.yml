name: Scrapers Test

on:
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'src/services/*.ts'
      - 'tests/*.test.js'
      - '.github/workflows/scrapers-test.yml'
  workflow_dispatch: # Allow manual trigger

jobs:
  test-all-scrapers:
    name: Test All Game Scrapers
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      pull-requests: write # Needed to comment on PRs

    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Use Node.js 20.x
      uses: actions/setup-node@v3
      with:
        node-version: 20.x
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Build TypeScript
      run: npm run build
    
    - name: Run All Scrapers Test
      id: test
      run: |
        echo "Running all scrapers test..."
        npm run test:scrapers > test-output.txt 2>&1
        TEST_EXIT_CODE=$?
        
        echo "Test exit code: $TEST_EXIT_CODE"
        
        # Save the output for the comment
        cat test-output.txt
        
        # Create a formatted output for the PR comment with collapsible details
        {
          echo "## 🎮 Game Scrapers Test Results"
          echo ""
          echo "**Timestamp:** $(date -u +"%Y-%m-%d %H:%M:%S UTC")"
          echo ""
          if [ $TEST_EXIT_CODE -eq 0 ]; then
            echo "✅ **Overall Status:** All scrapers passed"
          else
            echo "❌ **Overall Status:** Some scrapers failed"
          fi
          echo ""
          echo "<details>"
          echo "<summary>📋 Click to view detailed test results</summary>"
          echo ""
          echo '```'
          cat test-output.txt
          echo '```'
          echo ""
          echo "</details>"
          echo ""
          echo "---"
          echo "*This test runs all game scrapers (Epic, Steam, GOG, Amazon Prime) against their live websites.*"
          echo "*Manual review is recommended to confirm the listed games are actually free.*"
        } > pr-comment.md
        
        exit $TEST_EXIT_CODE
    
    - name: Comment PR with test results
      if: always() && github.event_name == 'pull_request'
      uses: actions/github-script@v7
      with:
        script: |
          const fs = require('fs');
          const commentBody = fs.readFileSync('pr-comment.md', 'utf8');
          
          // Find existing comment
          const { data: comments } = await github.rest.issues.listComments({
            owner: context.repo.owner,
            repo: context.repo.repo,
            issue_number: context.issue.number,
          });
          
          const botComment = comments.find(comment => 
            comment.user.type === 'Bot' && 
            comment.body.includes('Game Scrapers Test Results')
          );
          
          if (botComment) {
            // Update existing comment
            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: botComment.id,
              body: commentBody
            });
          } else {
            // Create new comment
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: commentBody
            });
          }
